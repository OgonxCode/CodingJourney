#include<iostream>


class electronicStore {
private:
	//string name city int max laptop & current laptop &laptop list


public:
	// no setter for num on laptops
	//contructor with int parameter for size of laptops in the store destructor for cloing the store to free the memory 


	//when you use the sell fuction replace the last brand with the one sold so it stays organized anything under it doesnt matter because the array is uknown anyway
};

int main() {

}