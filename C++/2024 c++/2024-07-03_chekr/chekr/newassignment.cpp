/*

#include <iostream>
using namespace std;

// Function prototype
void minMax(double& num1, double& num2);

int main() {
    cout << "Enter two numbers: ";
    double small, big;
    cin >> small >> big;

    // Call the minMax function to find the minimum and maximum
    minMax(small, big);

    // Output the results
    cout << "\nThe minimum number is: " << small << " and the maximum number is " << big << endl;

    return 0;
}

// Function definition for minMax
void minMax(double& num1, double& num2) {
    if (num1 > num2) {
        // Swap num1 and num2 if num1 is greater than num2
        double temp = num1;
        num1 = num2;
        num2 = temp;
    }
    // num1 is now the minimum, num2 is now the maximum
}
*/