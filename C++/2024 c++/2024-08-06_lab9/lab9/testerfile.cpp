#include<iostream>
#include<vector>

using namespace std;

int add(vector<int> Vec, int item);
int remove(vector<int> Vec, int index);
int set(vector<int> Vec, int index, int value);
int print(vector<int> Vec);


int main() {

	vector<int> mainVec;
	add(mainVec, 4);
	print(mainVec);

}

int add(vector<int> Vec, int item)
{
	return 0;
}

int remove(vector<int> Vec, int index)
{
	return 0;
}

int set(vector<int> Vec, int index, int value)
{
	return 0;
}

int print(vector<int> Vec)
{
	return 0;
}
